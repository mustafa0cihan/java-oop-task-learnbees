package org.example;

public interface ISoftwareDeveloper {

    // A SoftwareDeveloper can writeCode, testCode, pushChanges

    // First of all, writeCode(), testCode() and pushChanges() methods must be
    // defined in order to execute the following methods created in the Main class.

    // softwareDeveloper2.writeCode();
    // softwareDeveloper1.testCode();
    // softwareDeveloper2.pushChanges();

    public void writeCode();

    public void testCode();

    public void pushChanges();

}
